# Demo React and Pixi game test

## Description
This is only demonstration using Pixi in React app without using other dependencies.

## Development

- Install packages = `yarn install`
- Start development = `yarn dev`
- Build sources = `yarn build`
